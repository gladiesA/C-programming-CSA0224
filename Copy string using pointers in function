#include <stdio.h>
void copy(char *src, char *dest){
    while(*src) { *dest=*src; dest++; src++; }
    *dest='\0';
}
int main(){
    char src[]="Hello", dest[20];
    copy(src,dest);
    printf("Copied=%s",dest);
    return 0;
}
