#include <stdio.h>

struct Employee {
    char name[20];
    float basic, hra, total;
};

int main() {
    struct Employee e = {"John", 10000, 2000};

    e.total = e.basic + e.hra;

    printf("Total Salary = %.2f", e.total);
    return 0;
}
