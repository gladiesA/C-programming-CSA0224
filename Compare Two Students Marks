#include <stdio.h>

struct Student {
    char name[20];
    int marks;
};

int main() {
    struct Student s1 = {"Ram",80}, s2 = {"Sam",90};

    if(s1.marks > s2.marks)
        printf("%s has higher marks", s1.name);
    else
        printf("%s has higher marks", s2.name);

    return 0;
}
